CHIP And5
{
	//This function gives output of and operation of 5 inputs
  	IN a,b,c,d,e;
  	OUT out;
  
  	PARTS:
   	And(a=a,b=b,out=ab);
   	And(a=ab,b=c,out=abc);
	And(a=abc,b=d,out=abcd);
	And(a=abcd,b=e,out=out);
}